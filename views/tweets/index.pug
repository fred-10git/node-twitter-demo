extends ../layout

block content 
    script(src="/js/tweets/tweet.js")
    div(class='d-flex flex-row content mx-auto')
        include ../includes/profile
        div(class='flex-fill d-flex flex-column' id="tweet-list") 
            include tweets.pug

    